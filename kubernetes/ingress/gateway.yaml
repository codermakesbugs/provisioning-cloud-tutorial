apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devops-gateway
  namespace: devops-tools
spec:
  rules:
  - http:
      paths:
      - path: /jenkins
        pathType: Prefix
        backend:
          service:
            name: jenkins-service
            port:
              number: 8080